package newSt.StringOperations;

public class ZigZagConversion {

    public void zigZagMethod(String str, int rows){

        //int firstDifferenceBetweenElements = (rows -1)*2;

        int strLength = str.length();
        String jumbledStr = "";
        for(int i=0; i<strLength; i++){

            int charAt= 0;
            if(i==0||i==(strLength-1)){
                int firstDifferenceBetweenElements = (rows -1)*2;
            }

            //charAt =
            jumbledStr = jumbledStr + str.charAt(charAt);




        }


    }


}
